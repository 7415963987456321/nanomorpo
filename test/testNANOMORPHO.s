;;; NanoMorpho prufuskjal

main () {
    var john, cena, sum;
    var isCool;


    while (true) {
        if (6 == 7) {
            doStuff();
        };

        sum = 20 + 30 - 40;
    };

    return hamburgers;

}

fun(x) {
        while( x )
        {
            y = head(x) : y;
            x = tail(x);
        };
    y;
}

fun(f,lst)
{
        ;;; lst==null && (return []]);
            f(head(lst)) : map(f,tail(lst));
}

fun()
{
        var lst;
        var f;
       ;;; f = fun(x) { x*x; };

        writeln(map(f,lst));
}

fun(){
   t = 4 * 3;
    return 1:2:3:4:null;
}

fun(){
    if(1&&2){
        return 123;
    } elsif(1==2 || 3==4 && 1==1 && !test){
        return null;
    } else {
        return 1234;
        x = 1 && 2;
    };
}