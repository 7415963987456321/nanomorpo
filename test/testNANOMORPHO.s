;;; NanoMorpho prufuskjal

main () {
    var john, cena;
    var isCool;


    while (true) {
        if (6 == 7) {
            doStuff();
        };

        sum = 20 + 30 - 40;
    };

    return hamburgers;

}

fun(x) {
        while( x )
        {
            y = head(x) : y;
            x = tail(x);
        };
    y;
};

fun(f,lst)
{
        lst==null && (return []);
            f(head(lst)) : map(f,tail(lst));
};

main = fun()
{
        var lst;
        var f;
        f = fun(x) { x*x; };

        writeln(map(f,lst));
};